import { useEffect,useState } from 'react';import axios from 'axios';export default function Suggestions(){const [items,setItems]=useState([]);useEffect(()=>{axios.get((import.meta.env.VITE_BACKEND_URL||'http://localhost:5000') + '/suggestions').then(r=>setItems(r.data.suggestions||[])).catch(()=>setItems([]))},[]);return (<div className='p-6 max-w-5xl mx-auto'><h2 className='text-2xl font-bold mb-4'>AI Suggestions</h2><div className='grid grid-cols-1 md:grid-cols-3 gap-4'>{items.map((it,idx)=>(<div key={idx} className='bg-white rounded shadow p-3'><img src={it.image} className='w-full h-40 object-cover rounded' alt='preview'/><h3 className='font-semibold mt-2'>{it.name}</h3><p className='text-sm text-gray-600'>{it.description}</p></div>))}</div></div>)}
